# Checkpoint — Enlyce — 2026-09-04

## Resumen

Se cerró la sesión de exploración de la web pública de L&C Propiedad Raíz. Los tres prototipos quedaron guardados, verificados y documentados; el servidor local de revisión fue detenido.

## Cambios realizados

- Se creó `website/` como aplicación pública separada de `frontend/`, el CRM privado.
- Se entregaron tres ensayos: editorial premium, marketplace urbano y cercano paisa.
- Se documentaron patrones de Fincaraíz y Metrocuadrado, brechas del backend y tres stacks posibles.
- Se generaron recursos visuales originales de Medellín y propiedades.
- Se guardó el trabajo en el commit `d84a038 feat(web): crear prototipos publicos de lyc`.
- Se detuvo el servidor Python PID 22760 y se verificó que el puerto 4173 quedó cerrado.

## Estado actual

- HTML, CSS, JavaScript y referencias locales verificados.
- Los formularios, favoritos, filtros y búsqueda son demostraciones; no persisten datos.
- 133 pruebas de dominio y 39 de integración pasan.
- El build del CRM React/Vite pasa.
- No existe remoto Git configurado; el commit no puede enviarse.

## Pendientes / próximos pasos

- Elegir uno de los tres conceptos o aprobar la fusión recomendada.
- Entregar identidad real de L&C: logo, colores, fotos, WhatsApp, dirección y redes.
- Migrar el concepto elegido a Next.js + TypeScript.
- Extender Enlyce.Api con catálogo público, filtros, fotos, mapa, slugs SEO y captura de leads.
- Resolver vulnerabilidades conocidas de `System.Security.Cryptography.Xml 9.0.0` y revisar por qué `Enlyce.Application.Tests` no descubre pruebas.

## Decisiones y notas

- Dirección recomendada: estructura del concepto 02, estética del concepto 01 y tono del concepto 03.
- Stack recomendado: Next.js para web pública, React/Vite para CRM y ASP.NET Core como backend único.
- C, Java y Python quedan descartados para un backend nuevo porque duplicarían el dominio existente.
- Investigación competitiva reutiliza patrones funcionales; no copia identidad ni código.

## Git

- Rama: `master`.
- Último commit funcional: `d84a038`.
- Push bloqueado: no hay remoto configurado.
- Cambios previos del usuario en `docs/actividad-1/` permanecen intactos.

---
Tags: #checkpoint #enlyce #lyc-web
