# Checkpoint — Enlyce — 2026-09-12

## Resumen
Se corrigió el 405 que recibía el panel del CRM al consultar visitas. La API ahora ofrece un listado autenticado con alcance por rol, sin cambiar el contrato visual del frontend.

## Cambios realizados
- `IVisitaRepository` y su implementación EF recibieron `ObtenerVisitasAsync(Guid? asesorId)`; el filtro de asesor se ejecuta en SQL.
- `GET /api/visitas` devuelve todas las visitas al administrador y solo las propias al asesor; sin sesión devuelve 401.
- Tres pruebas de integración cubren ambos roles, autenticación y los campos JSON esperados.

## Estado actual
Build Release correcto, sin advertencias. Suite completa: 247/247 pruebas. La API local en 5019 ya ejecuta el cambio: login 200, listado de visitas 200 con array vacío, anónimo 401. El catálogo público sigue en 200.

## Pendientes / próximos pasos
- Las rutas heredadas `/api/visitas/lead/{id}` y `/api/visitas/asesor/{id}` siguen sin autorización; requieren una revisión de seguridad independiente.
- El cliente declara `getVisitaById`, pero no hay consumidores activos ni ruta backend; se dejó intacto.
- Poblar visitas reales para revisar el panel con datos.

## Decisiones y notas
El endpoint devuelve todos los estados y fechas; el panel ya filtra `Programada`. Se conservó esa responsabilidad para evitar cambiar el significado del listado general.

## Git
Rama `master`. Base: `a36ee30`. Commit del arreglo pendiente al redactar esta nota. Los documentos modificados antes de esta tarea no forman parte del cambio.

---
Tags: #checkpoint #enlyce
